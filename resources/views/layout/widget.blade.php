<div class="card">
    <div class="card-body">
        <p class="fs-4">
            <span class="{{ $icon ?? 'fa-solid fa-bars-progress' }}"></span>
            {{ $title ?? '' }}
        </p>
        <h2 class="fw-bold fs-2">{{ $totalCount ?? 0 }}</h2>
    </div>
</div>
